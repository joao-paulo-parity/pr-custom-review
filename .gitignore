# deps
node_modules

# Build artifacts
build/*
!build/package.json

# Private keys for GitHub apps
*.pem

# Environment files
.env.cjs

# jest
coverage

# ncc output directory used for the action release
# normally the package would be ignored, but in this case it shouldn't because
# it is committed for github tags in our release process
# dist
